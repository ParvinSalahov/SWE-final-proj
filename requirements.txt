# Dependencies needed to RUN the provided ai/ package.
# Add your own SE-layer dependencies (fastapi, asyncpg, tenacity, etc.)
# to your project's main requirements.txt.

numpy>=1.24
pydantic>=2.7
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
tenacity>=8.2.0
asyncpg>=0.29
sqlalchemy>=2.0
typer>=0.12.0
requests>=2.31.0
Pillow>=10.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Pick ONE or more depending on which provider(s) you intend to use.
# (None of these are imported until the corresponding provider is selected.)
anthropic>=0.39.0      ; extra == "anthropic"
openai>=1.50.0         ; extra == "openai"
google-genai>=0.3.0    ; extra == "gemini"

# Smoke tests
pytest>=8.0
